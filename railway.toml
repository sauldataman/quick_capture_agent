# Railway deployment configuration
[build]
builder = "dockerfile"

[deploy]
# Use CMD from Dockerfile - don't override with startCommand
# This allows Railway to use the container's default command
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
